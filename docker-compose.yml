version: '3.3'

networks:
  main:

services:
  naming-server:
    image: 'naming_server:latest'
    networks:
      - main

  storage-server-1:
    image: 'storage_server:latest'
    networks:
      - main
    environment:
      NAMING_SERVER_ADDRESS: 'naming-server:5678'

  storage-server-2:
    image: 'storage_server:latest'
    networks:
      - main
    environment:
      NAMING_SERVER_ADDRESS: 'naming-server:5678'



